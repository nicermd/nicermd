# Changelog

This file follows [Keep a Changelog](https://keepachangelog.com)
format and [Semantic Versioning](https://semver.org). Until the
first public release lands, the authoritative history is `git log`.

## Unreleased

- See `git log` for the running list of changes on `main`.
