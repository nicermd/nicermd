# Changelog

> Placeholder — version history begins at the first public release.

This file will follow [Keep a Changelog](https://keepachangelog.com) format
and [Semantic Versioning](https://semver.org). Until v0.1 ships publicly,
the authoritative history is `git log` and the tags `v0.1-milkdown`,
`v0.2-cm-prototype`, `v0.3-hybrid-foundation`, `v0.4-tauri-foundation`.

## Unreleased

- See `git log` for the running list of changes on `main`.
